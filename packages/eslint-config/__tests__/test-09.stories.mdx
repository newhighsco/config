import { Heading, Meta, Source, Typeset } from '@storybook/addon-docs'
import fonts from '@styles/_fonts.module.scss'
import { Fragment } from 'react'

export const sizes = ['1em', '2em', '3em', '4em']

<Meta title="Styles/Fonts" />

# Fonts

`import fonts from '@styles/_fonts.module.scss'`

<br />

<>
  {Object.keys(fonts).map(font => {
    return (
      <Fragment key={font}>
        <Heading>fonts.${font}</Heading>
        <Source code={`font-family: ${fonts[font]}`} language="css" dark />
        <Typeset fontFamily={fonts[font]} fontSizes={sizes} />
      </Fragment>
    )
  })}
</>
