version: 2.1

orbs:
  newhighsco: newhighsco/orb@1.3.0
  node: circleci/node@5.0.2

jobs:
  test:
    executor: node/default
    steps:
      - attach_workspace:
          at: .
      - run: yarn test
  release:
    executor: node/default
    steps:
      - attach_workspace:
          at: .
      - deploy:
          command: npx multi-semantic-release --sequential-init

workflows:
  version: 2
  test_and_release:
    jobs:
      - newhighsco/setup:
          name: setup
      - test:
          requires:
            - setup
      - release:
          requires:
            - test
          filters:
            branches:
              only:
                - main
